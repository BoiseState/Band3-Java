# Band3-Java

Build the project using NetBeans to create a contained application.

To properly build the installer, copy the project files outside of the git repo and run winInstaller.sh for Windows, and macInstaller.sh for Mac.
- the winInstaller must be run on a Windows machine.
- To create the the macInstaller, copy the text from the .sh file into the command line on a Mac machine.
